import React from "react";
import ReactDOM from "react-dom/client";
import Moneyy from "./code.jsx"; // your component
import './index.css'

ReactDOM.createRoot(document.getElementById("rootz")).render(
  <React.StrictMode>
    <Moneyy />
  </React.StrictMode>
);
